- 所有远程API调用均使用JDK原生的`HttpURLConnection`，并设置3秒连接超时。
- 业务异常统一包装为运行时异常`GenException`抛出，避免受检异常扩散。
- 数据Bean类均使用Lombok的`@Data`注解生成getter/setter/toString等方法。
- 文件操作优先使用NIO.2的`Files`类（如`Files.delete`, `Files.copy`），但ZIP解压仍使用传统IO流。