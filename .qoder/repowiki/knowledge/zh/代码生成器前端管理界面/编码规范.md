- API 模块按业务域拆分（如 project.js, datasource.js），统一导出请求方法并指定 HTTP 动词与路径。
- 视图组件采用 `<script setup>` 语法糖，通过 `ref` 定义响应式状态，并使用 `proxy` 访问全局属性。
- 全局工具函数（如 parseTime, resetForm）挂载至 `app.config.globalProperties`，在组件内通过 `proxy` 调用。
- 布局组件（Layout）采用动态插槽与侧边栏联动，支持 TagsView 多标签页导航与面包屑自动匹配。