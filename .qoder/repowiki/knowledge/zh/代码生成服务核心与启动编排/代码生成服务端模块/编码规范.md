- REST 控制器统一继承自 `Route` 接口定义的常量路径，并使用 `@RequestMapping(Route.PREFIX)` 设定基础前缀。
- 所有业务实体（Entity）、数据传输对象（DTO）和视图对象（VO）按类型严格分包存放于 `bean` 目录下。
- Service 层方法在处理多数据源查询时，显式调用 `DynamicDataSourceHolder.set()` 切换当前执行上下文的数据源。
- API 响应统一封装为 `R` 对象，异常处理倾向于抛出 `ValidationException` 或 `SystemException` 由全局处理器捕获。