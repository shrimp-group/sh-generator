- 采用Maven多模块架构（pom打包）统一管理`generator-server`、`generator-client`及`generator-server-starter`的生命周期与依赖版本。
- 前后端分离部署：`generator-ui`作为独立静态资源应用，通过HTTP API与基于Spring Boot的`generator-server-starter`交互。
- 构建期集成：`generator-client`作为Maven插件在`package`阶段触发，通过HTTP调用服务端接口获取生成内容并解压合并至本地工程，实现CI/CD流水线中的自动化代码注入。